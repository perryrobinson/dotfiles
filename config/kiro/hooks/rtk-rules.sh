#!/usr/bin/env bash
echo "# RTK - Rust Token Killer
Always prefix shell commands with \`rtk\`.
Examples: rtk git status, rtk cargo test, rtk npm run build, rtk docker ps
Meta: rtk gain (savings analytics), rtk proxy <cmd> (raw command without filtering)"
